# Context
<!-- Updated by /wrap at session end. Edit manually if needed. Keep it under 8 lines. -->

**Current focus:** Greek V1 visual polish; normalize_images.py not yet run on full image set; real-device sign-off pending
**Last session:** Visual QA infra (Playwright screenshots, scorecard template); fixed nav clipping on short viewports (max-height breakpoints); removed image frame; built normalize_images.py (flood-fill background normalizer); content fixes (romanization, image descriptions).
**Blocking:** normalize_images.py needs a full run + spot-check before sign-off
**Next action:** `make normalize-images` on all images, verify shadow preservation, then real-device testing for Greek V1 sign-off
<!-- wrapped: 2026-05-07 -->
